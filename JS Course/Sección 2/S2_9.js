// Null and Undefined

console.clear()

var mVar
console.log(mVar) //Sale "undefined" ya que la variable no tiene un valor

mVar = null //La variable tiene el valor nulo "null" para representar que no tiene valor
console.log(mVar)